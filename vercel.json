{
    "builds": [{
        "src": "Django-Currency-Converter/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "Django-Currency-Converter/wsgi.py"
        }
    ]
}